"use client"

import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    palette: {
        mode: 'light',
        primary: 
        {
           main: "#000"
        },
        secondary: 
        {
            main: "#fff"
        },
        info:
        {
            main: '#526BA1',
        }
    }
})